SQLITE3 lotto.db

CREATE TABLE previous_numbers(
id INTEGER PRIMARY KEY AUTOINCREMENT,
prev_number TEXT NOT NULL UNIQUE,
date TEXT NOT NULL);

CREATE TABLE my_numbers(
id INTEGER PRIMARY KEY AUTOINCREMENT,
my_num TEXT NOT NULL UNIQUE,
date TEXT NOT NULL);
